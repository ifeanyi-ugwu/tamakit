# TODO

1. in building the doc page, i needed to add react native web to both the registry and the website itself, why is that

2. after building the cli, check if the dependencies used to build it should all actually be dev ones or to keep it

3. react native ^0.77.1 is the one that supports react 18 cus tamagui does not support react 19 yet
